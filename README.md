# RAG Realized by Hand

A hand-built RAG application in Python.

The source code is in app.py. Configure API credentials locally in a .env file; secrets are intentionally excluded from this repository.
